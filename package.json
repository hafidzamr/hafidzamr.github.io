{
  "name": "personal-web",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://hafidzamr.github.io/",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && gh-pages -d out -t true"
  },
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "gh-pages": "^3.1.0",
    "next": "10.0.6",
    "next-compose-plugins": "^2.2.1",
    "next-images": "^1.7.0",
    "postcss": "^8.2.4",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "tailwindcss": "^2.0.2"
  }
}
